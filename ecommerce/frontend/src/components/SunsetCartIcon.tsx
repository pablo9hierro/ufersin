// Uiverse.io by abhinav_7137 — só o carrinho SVG (os 5 itens caindo
// foram removidos; a fumaça que subia no botão de WhatsApp agora sobe
// em cima desse ícone no CartFab).
export default function SunsetCartIcon({ scale = 1 }: { scale?: number }) {
  return <div id="cart-icon" style={{ transform: `scale(${scale})` }} />
}
