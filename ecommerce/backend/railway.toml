[build]
builder = "RAILPACK"
watchPatterns = []

[deploy]
startCommand = "./target/release/sonset_backend"
healthcheckPath = "/internal/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
